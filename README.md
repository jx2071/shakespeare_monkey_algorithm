# shakespeare_monkey_algorithm

Run with VSCode LiveServer

In URL Parameter, add "target" param to specify desired string.

If no "target" param exist, the default is "To be or not to be that is a question?".
